@model AnimalDB.Repo.Entities.Sop

@section BackNav {
    <a class="btn btn-sm btn-secondary" href="@Url.Action("Index", new { id = Model.Category_Id })"><i class="fas fa-arrow-left"></i> Back to S.O.P. Category</a>
}

<h2>@Html.DisplayFor(model => model.Description) @Html.ActionLink("Download", "GetSop", new { id = Model.Id, download = 1 }, new { @class = "btn btn-outline-dark" })</h2>

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.FileName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FileName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DateUploaded)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DateUploaded)
        </dd>

    </dl>
</div>
<hr />

<div class="row">
    <embed src="@Url.Action("GetSop", "Sops", new { id=Model.Id })" class="offset-md-1 col-md-10" height="842" />
</div>