@model AnimalDB.Repo.Entities.ClinicalIncidentReport

@section BackNav {
    <a class="btn btn-sm btn-secondary" href="@Url.Action("Index", new { id = Model.Animal_Id })"><i class="fas fa-arrow-left"></i> Back</a>
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this Clinical Incident Report?</h3>
<div>
    <hr />
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn  btn-outline-danger" />
        </div>
    }
</div>
