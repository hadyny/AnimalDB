@model AnimalDB.Repo.Entities.RosterNote

<h3>@Html.ActionLink("Back", "Index", new { id = Model.Roster_Id }, new { @class = "btn btn-primary" })</h3>


<h3>Are you sure you want to delete this note?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Content)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Content)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-outline-danger" />
            @Html.ActionLink("Cancel", "Index", new { id = Model.Roster_Id }, new { @class = "btn btn-outline-dark " })
        </div>
    }
</div>
